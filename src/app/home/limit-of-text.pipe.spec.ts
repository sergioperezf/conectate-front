import { LimitOfTextPipe } from './limit-of-text.pipe';

describe('LimitOfTextPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitOfTextPipe();
    expect(pipe).toBeTruthy();
  });
});
